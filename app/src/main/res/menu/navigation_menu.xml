<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <group
        android:id="@+id/group1"
        android:checkableBehavior="single">

        <item
            android:id="@+id/nav_home"
            app:actionLayout="@layout/nav_menu_item_home"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_category"
          app:actionLayout="@layout/nav_menu_item_category"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_order"
            app:actionLayout="@layout/nav_menu_item_order"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_favourite"
            app:actionLayout="@layout/nav_menu_item_favourite"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_cart"
            app:actionLayout="@layout/nav_menu_item_cart"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_accounts"
            app:actionLayout="@layout/nav_menu_item_accounts"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_darkmode"
            app:actionLayout="@layout/nav_menu_item_darkmode"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_shareApp"
            app:actionLayout="@layout/nav_menu_item_darkmode"
            tools:ignore="MenuTitle" />

        <item
            android:id="@+id/nav_log"
            app:actionLayout="@layout/nav_menu_item_log"
            tools:ignore="MenuTitle" />

    </group>

</menu>